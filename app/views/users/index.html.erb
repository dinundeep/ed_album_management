
<marquee><font color="red"><h2>EMPLOYEE'S INFORMATION</h2></font></marquee>
<center>
 <table border="2">
  <tr>
    <th>Login name</th>
    <th>First name</th>
    <th>Last name</th>
    <th>Login role</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

   <% @users.each do |user| %>

   <tr>
    <td><%= user.login_name %></td>
    <td><%= user.first_name %></td>
    <td><%= user.last_name %></td>
    <td><%= user.login_role %></td>
    <td><%= user.email_id %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
   </tr>
  <% end %>
 </table>
</center>
<br />

<a href="/home/admin">Back</a>
